context abs_ctx

sets

	/* models all possible Health Records the system could have. */
	MY_HEALTH_RECORD

	/* models all possible people of the system (e.g. a consumer). */
	PEOPLE

	/* models all possible health service providers of the system (e.g. a hospital). */
	SERVICE_PROVIDERS

axioms

	@axm1: "finite(MY_HEALTH_RECORD)"

	@axm2: "finite(PEOPLE)"

	@axm4: "finite(SERVICE_PROVIDERS)"

end